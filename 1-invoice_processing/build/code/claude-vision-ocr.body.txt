# Claude Vision OCR

={{ JSON.stringify({ model: 'claude-sonnet-5', max_tokens: 1500, output_config: { effort: 'low' }, system: $('Re-attach PDF Binary').item.json.ocr_system_prompt, messages: [ { role: 'user', content: [ { type: 'document', source: { type: 'base64', media_type: 'application/pdf', data: ($json.pdf_base64 || $json.data) } }, { type: 'text', text: 'Read this scanned invoice and return the flat JSON object defined in the system prompt. Email subject: ' + ($('Re-attach PDF Binary').item.json.subject || '') + '. Filename: ' + ($('Re-attach PDF Binary').item.json.attachment_filename || '') + '.' } ] } ] }) }}
